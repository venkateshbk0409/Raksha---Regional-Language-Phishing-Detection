# Backend API URL (for production deployments on Vercel)
# In local dev: leave unset to use Vite's local dev proxy (http://127.0.0.1:8000)
# In production on Vercel: set to your deployed Render backend URL (no trailing slash)
# VITE_API_URL=https://raksha-backend.onrender.com
